// Mod >> User

const partPrev = '';
const partNext = '';

var partInit = new partPrev;
var partExec = new partNext;


const newFunc = new function tempFunc(pars) {
  
}(arguments);

const newFunc = new function => (vals){
  let var1 = vals;
  return this(var1);
};


/*
var prevParticipation = new function => (vals){
    vals =   
  }
  lastParticipation()

  lastParticipation 
  to
  newParticipation



function lastParticipation => (valPrev){

}

{
  let Mod = initInvite => User (lastParticipation)
}  
  


User -> Response (to invite)

mInvite | uResponse1

Mod followUp = (case) -> User (lastParticipation = break)

uResponse1 | mReply1
*/

Function test = 

const testFunc = Funct test(params) {
  let x = params;
  return this(x)
}(arguments); ()



Function testFunc2 = testFunc2(params){
  tempFunc
}

var testFunc2 = new Function(({
  funcTemp => (){
        tempFunc1 => testFunc2(params){
          let x = params;
          return this(x)
        } 
      }
    )
  )
};


  Mod >> User
  send && receive
Or:
  Mod >> User 
  case: {
    param1 || prevParticipation || lastParticipation || 
  }
  then break:
